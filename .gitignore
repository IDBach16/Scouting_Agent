.env
synergy_token.json
*.log
__pycache__/
*.pyc
*.pt

# Safety backup written by awre_pull.py on every run — regenerates itself,
# and it is a full copy of awre_data.csv so committing it would double the repo.
*.bak
*.tmp
